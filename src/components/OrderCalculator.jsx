export default function OrderCalculator({ orderItems, pieces, costs }) {
  return <section className="summary-card"><h2>Total del pedido</h2>{orderItems.filter((item) => item.quantity).map((item) => <div className="cost-row" key={item.furnitureType}><span>{item.label} ({item.quantity})</span><b>{(pieces.filter((piece) => piece.furnitureType === item.furnitureType).reduce((sum, piece) => sum + piece.areaCm2, 0) / 10000).toFixed(2)} m²</b></div>)}<div className="cost-total"><span>Costo total de melamina</span><b>{costs.cost.toFixed(2)} Bs</b></div><div className="cost-row"><span>Valor utilizado</span><b>{costs.usedValue.toFixed(2)} Bs</b></div><div className="cost-row"><span>Valor recuperable</span><b>{costs.recoverableValue.toFixed(2)} Bs</b></div><div className="cost-row"><span>Costo del desperdicio</span><b>{costs.wasteCost.toFixed(2)} Bs</b></div></section>;
}
